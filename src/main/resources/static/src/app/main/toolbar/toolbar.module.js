(function ()
{
    'use strict';

    angular
        .module('app.main.toolbar', [
        	'flow'
        	])
        .config(config);

    /** @ngInject */
    function config()
    {
        
    }
})();
