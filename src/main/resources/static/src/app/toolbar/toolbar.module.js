(function ()
{
    'use strict';

    angular
        .module('app.toolbar', [
        	'flow'
        	])
        .config(config);

    /** @ngInject */
    function config()
    {
        
    }
})();
